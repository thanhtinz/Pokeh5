# --- secrets --------------------------------------------------------------
# The original release committed the MySQL root password in connect.php.
portal/config/config.php
deploy/.env
*.pem
*.key

# --- release contents (not ours to redistribute) ---------------------------
# The Egret client (~189 MB) and the .NET services (~2.6 GB) come from the
# release assets. See docs/SETUP.md for where to put them.
app/www/
app/client-boot.png
client/
server/
XJLserver/

# Database dumps carry the previous operator's account data.
deploy/mysql/init/*.sql
!deploy/mysql/init/*.sql.example

# --- build output ----------------------------------------------------------
node_modules/
app/android/
app/ios/
dist/
screenshots/

# --- local -----------------------------------------------------------------
*.log
.DS_Store
.cache/
